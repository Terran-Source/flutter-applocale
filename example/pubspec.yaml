name: applocale_example
description: Flutter Plugin Demo
publish_to: none

version: 0.1.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  applocale:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - i18n/
